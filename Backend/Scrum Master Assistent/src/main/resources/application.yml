spring:
  application:
    name: scrum-assistant-ai

  # 1. Database Configuration (Normal SQL Mode)
  datasource:
    # Use the default 'postgres' DB so you don't have to create a new one
    url: jdbc:postgresql://localhost:5432/postgres
    # Use your actual Mac username to avoid the 'Role does not exist' error
    username: srilaharisivanvithanori
    password: ""
    driver-class-name: org.postgresql.Driver

  # 2. JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update # Automatically creates the 'technical_debt' table for you
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # 3. AI Configuration (Simplified)
  ai:
    openai:
      api-key: sk-proj-51u16... # Paste your key directly here
      chat:
        options:
          model: gpt-4o

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG